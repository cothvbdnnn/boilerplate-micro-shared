const state = {
  loadingPage: true,
};

const getters = {
  loadingPage: (state) => {
    return state.loadingPage;
  },
};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
